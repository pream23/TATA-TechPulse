# Assignment 2 - Simulated Driving Agent

A compact evolutionary simulation. Each agent has a neural policy mapping vehicle state to steering/throttle. Genetic selection, crossover and mutation improve the population score.

Run `python driving_agent.py`.
