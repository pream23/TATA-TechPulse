# Assignment 8 - Sentiment Analysis using LSTM

PyTorch text classifier with an embedding layer and LSTM. Provide a CSV with `text` and binary `label` columns for real training; the script includes a tiny demonstration dataset.
