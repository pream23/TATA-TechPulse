# Assignment 5 - Predictive Maintenance

Binary classification for machine failure from sensor-style features. The script handles class imbalance with class-weighted Random Forest.

Expected target: `machine_failure`. If no CSV is provided, a reproducible synthetic sensor dataset is used.
