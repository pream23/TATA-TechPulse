# Assignment 6 - Traffic Sign Classification

CNN training template for a folder-structured traffic-sign dataset.

Dataset layout:
`data/train/<class>/*.png`

Run `python traffic_sign_cnn.py --data data/train`.
