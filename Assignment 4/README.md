# Assignment 4 - Vehicle Price Prediction

Compares regression approaches. Supply a CSV containing a numeric `price` target; otherwise a synthetic dataset is generated.

Run `python vehicle_price_prediction.py`.
